import { Row } from 'reactstrap';

export default () => (
    <Row style={{backgroundColor: '#4f4f4f', color: '#FFF'}}>
        <div style={{padding: '50px'}}>
            <small>By Philip Tietjen</small>
        </div>
    </Row>
)
